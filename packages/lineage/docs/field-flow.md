# QRP join-key field flow (generated)

_Auto-generated by `lineage.diagram` — the free substitute for the Dagster+ column-
lineage view. Each flowchart shows how a join key propagates across the tables that
carry it (edges = declared + auto-derived transform + FK referential). Regenerate with_
`uv run python -m lineage.diagram`.

> Note: only edges where **both** endpoints carry the key are shown — a transform that
> drops/renames the key terminates the flow, so absence of an edge here is not absence of
> lineage (see the full Dagster asset graph for that).

### `composite_figi` field flow

```mermaid
flowchart LR
    fact_returns[fact_returns]
    fundamentals[fundamentals]
    gics_scd[gics_scd]
    pageview[pageview]
    portfolio_weight[portfolio_weight]
    prices_raw[prices_raw]
    score[score]
    securities[securities]
    security_names[security_names]
    security_symbology[security_symbology]
    universe_member_resolution[universe_member_resolution]
    universe_membership[universe_membership]
    weight[weight]
    wiki_map[wiki_map]
    fact_returns --> score
    fact_returns --> weight
    fundamentals --> score
    fundamentals --> weight
    prices_raw --> fact_returns
    securities --> fact_returns
    securities --> fundamentals
    securities --> gics_scd
    securities --> portfolio_weight
    securities --> prices_raw
    securities --> security_names
    securities --> security_symbology
    security_symbology --> pageview
    security_symbology --> weight
    security_symbology --> wiki_map
    universe_member_resolution --> universe_membership
    universe_membership --> score
    universe_membership --> weight
    wiki_map --> pageview
```

### `sym_id` field flow

```mermaid
flowchart LR
    fact_index_returns[fact_index_returns]
    index_levels[index_levels]
    instrument[instrument]
    index_levels --> fact_index_returns
    instrument --> fact_index_returns
    instrument --> index_levels
```
