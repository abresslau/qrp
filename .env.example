# Copy to .env and fill in. .env is gitignored — never commit real credentials.
# QRP reads the SAME sym database, using sym's own env convention.

# Either a single DSN (wins if present)...
# SYM_DATABASE_URL=postgresql://postgres:password@localhost:5432/sym

# ...or the discrete parts (each has a local-dev default):
SYM_DB_HOST=localhost
SYM_DB_PORT=5432
SYM_DB_NAME=sym
SYM_DB_USER=postgres
# SYM_DB_PASSWORD=

# Where the console's server-side fetches reach the API (default below):
# API_BASE=http://127.0.0.1:8000
