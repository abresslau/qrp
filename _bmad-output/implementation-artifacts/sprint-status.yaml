# generated: 2026-06-13
# last_updated: 2026-06-13
# project: QRP
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# DERIVATION NOTE (2026-06-13):
# ==============================
# Statuses derived from the [BUILT]/[NEW] epic tags and the `Status:` field in the
# implementation-artifacts story files (this project tracks state in epics + code, not a
# live status file). development_status is enumerated strictly from the six *epic*.md files.
# Additional delivered stories exist as artifacts but are NOT in the epic decompositions
# (e.g. 1.9/1.10, U3.5-U3.7, B1-B7, QL1-QL5, O.2-O.4, S.1, A.1, C.1/C.2) and are tracked
# inline in those epics' build-status sections + deferred-work.md, not here.

generated: 2026-06-13
last_updated: 2026-06-24  # story fx-package created (Andre: "I want to do that also for fx") — sibling to universe-package in the sym-decomposition program. Extract FX into a full peer package WITH its own `fx` database. SIMPLER than universe: no inbound FKs, fx is read-only from sym -> no circular dep / no inversion. Hard parts: the fx_rate->currency FK (seed currency in the fx DB) + route reads at the fx DB (rate-map fetch, not cross-DB join). Mapped fx coupling first (Explore). Spec: fx-package.md. Earlier today: universe-package created + revised to approach B (own `universe` database; invert the circular dep via injected Resolver; roster-fetch cross-DB; universe_benchmark stays in sym). Spec: universe-package.md. Earlier: data-monitor-compact-counts code-reviewed -> done (AC1-7 met; 3 patches: dead link, false "single-day" comments, note->tooltip; 1 defer, 3 dismiss; CDP no-scroll re-verified). Earlier: dev complete (bmad-dev-story) -> review: de-tabbed board at /data-monitor + per-bucket trailing-window instrument counts + new commodities bucket (collision-guarded) + two-tier compaction (CDP no-scroll @1440x900 & 1366x768). 208 api+lineage green, api-types regenerated. Earlier: menu-title-case code-reviewed -> done (clean; 12 platform.toml name fields Title-Cased incl. sym->Sym, 0 key changes, live /api/platform verified). Earlier: story menu-title-case created (bmad-create-story, Andre): Title-Case the rail module labels via platform.toml names (incl. sym->Sym per Andre's choice 2; keys/routes unchanged; explicit strings not a blanket transform). Spec: menu-title-case.md. Earlier: story data-monitor-compact-counts created (bmad-create-story) -> ready-for-dev: compact the Data Monitor board (no-scroll, two-tier density) + per-bucket single-day instrument counts (fx/rates/commodities/...) + add a commodities bucket (collision-guarded vs the existing commodities Dagster job) + remove the lone EOD tab (board at /data-monitor). Spec: data-monitor-compact-counts.md. Earlier: /backtest Overfitting-sweep panel (DSR/PBO/MinBTL) shipped + commodities.price_daily & backtest.sweep schemas deployed via Docker sqitch (both follow-ups from the 3-thread merge now closed). Earlier: merged to main 2026-06-24. rates-sources (Fed GSW + ECB euro-area expansion) code-reviewed (bmad-code-review, Blind + Edge layers; no-spec so Auditor skipped) -> done. NEW US Fed GSW fitted-curve source (feds200628 nominal + feds200805 TIPS: spot/par/forward over nominal/real/inflation, curve_set='gsw', 60yr history, pure-parse + lazy download); ECB EcbYieldCurveSource expanded from AAA-spot-only to 2 universes (govt G_N_A + govt_all G_N_C) x 3 rate types (spot SR/forward IF/par PY) x 12 tenors = 72 series; build_registry() retyped dict[str,CurveSource]->dict[str,list[CurveSource]] (US = [Treasury CMT, Fed GSW]); cli load-world attempt-all per (country,source); source provenance plumbed through gateway/router (Curve/CompareCurve.source) + page (source + curve-set labels, gsw/govt_all in CS_RANK); page cross-country overlay moved onto the main chart + valueAtTenor interpolation fixed to linear-in-sqrt(tenor) (matches the sqrt-scaled x axis). Registry blast-radius verified CLEAN (only value-consumer is load-world, updated). 1 patch (RP1: ECB per-series tolerance widened to CurveLayoutError/ValueError so a garbled-200 or bad-decode single series no longer aborts all 72 + a wholesale-outage raise so a full outage isn't a silent no-op; +2 tests). 3 deferred (deferred-work.md), 3 dismissed (incl. a false-positive 'overlays shown during timelapse' — the lines memo early-returns on `movie`). 57 rates tests green (+2), ruff clean. CAVEAT: web tsc/eslint/vitest not runnable locally (page reviewed by inspection, no Next API surface); api-types regen + live verification pending :8001 restart; house Docker-sqitch deploy unaffected (no schema change). Earlier: next-level-simulations Tier-1 REMAINDER (DSR + PBO/CSCV + MinBTL + backtest.sweep infra, 1B-steps-2..4) code-reviewed (bmad-code-review, 3 adversarial layers) -> done. New backtest/stats.py (pure-Python: norm_cdf/ppf Acklam, PSR, expected_max_sharpe=False-Strategy-Theorem, deflated_sharpe, pbo=CSCV S=16, min_backtest_length_years) + backtest/sweep.py (run_sweep: grid -> per-config runs aligned on common days -> DSR/PBO/MinBTL verdict, persisted backtest.sweep parent linking each run via run.sweep_id) + engine return_daily (headline net-when-costed series) + gateway/router (POST /api/backtest/sweep, GET /sweeps[/{id}]). Acceptance Auditor: ALL 6 spec items MET (formulas faithful, per-period SR, Euler-Mascheroni, N from sweep, S=16/0.05, annualised MinBTL, conservative-N labelled). 2 patches: BP1 run_sweep attempt-all per config (an unexpected exception no longer orphans already-committed runs); BP2 PBO tie-aware average rank (sorted().index() forced ties to stable-sort-worst -> spurious PBO~1.0; no-op for distinct series). 6 deferred (deferred-work.md), 6 dismissed (incl. false-positive MinBTL unit flag: 252 trading days ~= 1 calendar year). 39 backtest tests green, ruff clean. CAVEAT: /backtest page surfacing of the stats is a follow-up (backend+API only); house Docker-sqitch deploy of the sweep migration pending. Spec: next-level-simulations.md. (Tier-1 1A/1B-1/1C shipped 89b0f6f/4291e1f; this closes the Tier-1 remainder.) Earlier: commodities-store code-reviewed (bmad-code-review, 3 adversarial layers: Blind Hunter / Edge Case Hunter / Acceptance Auditor) -> done. NEW `commodities` peer package (2nd per-package instantiation after rates), built autonomously from the 2026-06-23 brainstorm v1 spec: Tier-A yfinance `=F` continuous front-month (OHLCV+Volume, no OI), own `commodities` DB, canonical as_of_date + immutable first_settle/restated settle PIT, 22 codes across all 6 sectors, load/coverage/validate verbs, + `/commodities` sector-grouped monitor page (board/heatmap/history via lib/date-axis). Triage 0 decision / 6 patch (applied) / 6 defer / ~9 dismiss, no unresolved High/Med: P1 broaden UPSERT restate guard to all OHLCV (settle-only dropped corrected OHLC/vol); P2 add Sqitch sqitch.conf+plan+revert+verify trio + register in tools/deploy_all.py (F1); P3 add Dagster `commodities` job + `commodities_daily` schedule (explicit tz America/New_York, STOPPED) + definitions.py reg (F2); P4 yfinance warn when a ticker returns rows but keeps 0 (column-shape/all-NaN silent total loss); P5 CLI drop no-op band tautology + reject band<=0 + close load conn in finally; P6 page fmtPrice decimal tier + "Latest as-of"=max-not-first. Deferred (deferred-work.md): band-baseline-never-advances (opt-in, matches rates), 430-day window can blank 1Y/YTD for a lagging commodity, naive date.today() in validate, leap-year chart cutoff, no source registry (single source), 22<25-30 codes. Dismissed: DSN/.env (byte-identical to rates), audit-verb (=coverage), 1D=prior-session, several verified-handled. Verified: 9 commodities tests green, ruff clean on all touched files, lineage.definitions loads with commodities_daily registered. CAVEAT: web tsc/eslint/vitest not runnable locally (page reviewed by inspection, no Next API surface); api-types regen + live page->API pending :8001 restart. Spec: commodities-store.md + brainstorming-session-2026-06-23-224317.md. Earlier: dagster-job-buckets code-reviewed (3 adversarial layers) -> done: 10 patches applied (fail-fast/attempt-all/never-500/Part-A tests) + 3 deferred + 1 dismissed; 44 lineage+EOD + 169 api green. Earlier: dagster-job-buckets dev complete (autonomous) -> review: Data Monitor EOD page (new area, supersedes+removes sym Overview) + nine config-driven Dagster bucket jobs, on one shared taxonomy; 191 api+lineage + 841 sym tests green, page verified, EOD running to refresh data. Earlier: story dagster-job-buckets created (bmad-create-story) -> ready-for-dev. Seven config-driven Dagster bucket jobs (fx/equity_prices/index_levels/rates/fundamental/alt_data/calculations), each with a launchpad-selectable subcategory list (default=all) + as_of_date, generalising the lineage trigger-only schedule pattern; subcategory lists discovered (universe/country), not hardcoded; attempt-all per-subcat. Dagster-only, no web/schema. Earlier: rates-curve-analytics code-reviewed (3 layers) -> done. 4 patches (added rates/layout.tsx tab strip, clamp OIS basis fetch, rename window->histWindow, fix stale checks.py docstring), 3 deferred, rest dismissed; ruff clean, 39 py tests, /rates HTTP 200 + dump-dom render. Caveats: LIVE page data pending :8001 restart; only latest-bundle (15d) loaded, full-history backfill deferred. Earlier: rates-curve-analytics dev complete -> review (derive-on-read trading layer over rates.curve_point: BoE conventions pinned + fwd-spot made exact-FAIL; analytics.py spreads/carry/DV01; spreads()+history gateway w/ z-score; /api/rates/spreads + /spread/{key}; app/rates/page.tsx curve chart + spread monitors. ruff clean, 39 py tests, validate exit 0, /rates HTTP 200 + dump-dom render. Caveats: web tsc/eslint/vitest not runnable locally [incomplete install], api-types regen + live page->API pending :8001 restart). Earlier: story rates-curve-analytics created -> ready-for-dev. Derive-on-read trading layer: pin BoE conventions+exact fwd-spot (gate) -> derive module (spreads/carry-roll/DV01) -> history+z-score gateway -> /api/rates/spreads API -> rates console page. Persist nothing; bond ref-data/live/2nd-source/multi-country OUT. Earlier: uk-rates-curve-store code-review pass 2 (re-run) -> still done: Edge 0/0/0 + Auditor acceptance-clean + Blind 0-High; 2 small patches (hole-guard FAIL->WARN +test; doc-debt cleanup); ruff clean, 23/23 tests, validate exit 0. Earlier: code-reviewed (3 layers) -> done. 1 decision resolved (keep single-row two-vintage model), 7 patches applied (api dep closure + deploy_all registry + workbook-close + tail-gate/--archive decouple + missing-tenor hole guard + gitignore caches + test fix), 8 deferred, 6 dismissed; ruff clean, 23/23 tests, live validate exit 0. Earlier: uk-rates-curve-store dev complete -> review (NEW `rates` package built end-to-end: BoE UK yield curves, own DB, probe-corrected schema spot+forward-no-par, 13,140 real nodes loaded, all 4 validate checks pass; caveats: sqitch/api-restart/api-types/backfill deferred). Earlier 2026-06-22: story uk-rates-curve-store created (bmad-create-story, after FI-curves brainstorm) -> ready-for-dev. NEW fixed-income asset class: a trustworthy PIT UK rates curve store. Topology decided (Andre, AskUserQuestion): NEW `rates` package peer to sym/macro (NOT in sym). Source = Bank of England daily yield curves (OGL Excel/zip). Scope = probe(GATE)->scaffold->curve_point grid schema(two vintages, tenor-as-data, canonical as_of_date)->BoE ingest(atomic per-day, layout-assert, plausibility)->validate(reconcile free-check/inflation=nom-real RPI/band/stale-gate)->London-EOD schedule(explicit tz)->minimal read API. Store BoE fitted grid verbatim+derive-on-read. Follow-ons OUT: derived analytics(spreads/carry/DV01+page), bond ref-data, live mark, 2nd-source divergence, multi-country. Earlier 2026-06-22: portfolio-returns-skip-gated code-reviewed (3 layers) -> done (clean: all ACs pass, no High/Med; Blind Hunter "High" was a false positive; 0 patches, 1 defer = behavioral test needs SQL-capable fake). Earlier 2026-06-22: portfolio-returns-skip-gated dev complete -> review (Andre: top-10 YTD not populated; AR-9 gating on /api/portfolios/{id}/returns -> pin to last real-returns date via `pr IS NOT NULL`; MTD/YTD 3->100/100; API 166 green). Earlier 2026-06-22: ticker-region-detail-pivot code-reviewed (3 layers) -> done (clean: all 5 ACs pass, no High/Med, column-alignment chain verified; 0 patches, 1 defer = per-row pivot store subscription). Earlier 2026-06-22: ticker-region-detail-pivot dev complete -> review (detail/pivot fast-follow: shared convention store + QualifiedTicker island; detail h1 + pivot Ticker column qualified; detail+composition APIs carry the codes; no migration; API 165 + web 150 green; CDP detail "ADS GR" + pivot "SNDK US"). Earlier 2026-06-22: ticker-region-codes code-reviewed (3 layers) -> done (3 patches: validate stored convention, trim codes, +Explorer render test; AC#7d closed; Blind Hunter "High" hydration was a false positive; web 150 green; 4 defers incl. venue-code accuracy + detail click-through). Earlier 2026-06-22: ticker-region-codes dev complete -> review (default Bloomberg Region; added bbg_exchange_code venue to exchange [exch_code/country_iso reused], ticker.ts helper, securities API codes, Explorer convention selector + qualified ticker; 31/35 venue seed, 4 NULL flagged; Docker down so sqitch deploy pending; API 165 + web 145 green; CDP ADS->ADS GR). Earlier 2026-06-22: story ticker-region-codes created (bmad-create-story, "show ticker with exchange+region; Bloomberg/FactSet") -> ready-for-dev (exch_code already = Bloomberg region + country_iso already = FactSet region; add only bbg_exchange_code venue code + a derived 3-convention qualified-ticker helper + Explorer/pivot/detail display w/ convention selector). Earlier 2026-06-22: portfolios-live-returns-fix code-reviewed (3 layers) -> done (all 6 ACs pass, no High/Med; Blind Hunter's 2 big findings were false positives refuted by read-access layer; 0 patches, 2 minor defers). Earlier 2026-06-22: portfolios-live-returns-fix dev complete -> review (A: 1D Chg -> live_return; B: composition skip-null pr -> windows 3->100/100; C reframed as intended AR-9 gating not a bug, sym recompute ran, operator-review follow-up). CDP-verified 1D Chg 101/101 + ticks 39/101. Earlier 2026-06-22: story portfolios-live-returns-fix created (bmad-create-story, "1D Chg not moving, only 3 names") -> ready-for-dev (3 causes: (A) grid 1D Chg reads window_returns not live_return; (B) composition() takes latest fact_returns row even when pr null -> 3/100 vs 100/100; (C) fact_returns 2026-06-18 pr null for 97/100, repair via sym recompute). Earlier 2026-06-22: portfolios-live-autorefresh-parity dev complete -> review (ported autoSec auto-refresh + refreshed-stamp + useOnline pause onto the /portfolios/[id]/live cockpit; 138 web green; CDP-verified the 3s tick advances the stamp). Earlier 2026-06-22: story portfolios-live-autorefresh-parity created (bmad-create-story, "portfolio live page like wei and fx") -> ready-for-dev (add autoSec auto-refresh + refreshed-stamp + useOnline pause to the /portfolios/[id]/live cockpit, mirroring heatmap-view/WEI/FX; no toggle, keep the pill badge). Earlier 2026-06-22: fx-matrix-live code-reviewed (3 adversarial layers) -> done (3 patches: null chg on stale-leg cells, exclude USD from priced/total so all-miss reads unavailable, +3 rollup-boundary tests; 165 api green, live API 15/15; both "High"s dismissed). Earlier 2026-06-22: fx-matrix-live dev complete -> review (GET /api/sym/fx/matrix/live: USD{ccy}=X live legs, crosses re-derived via shared _fx_grid, EOD byte-identical; LIVE/EOD toggle + badge + per-ccy freshness + auto-refresh on /monitor/fx; 163 api + 135 web green; CDP-verified 16/16 priced). Earlier 2026-06-22: story fx-matrix-live created (bmad-create-story, "FX matrix live similar to wei page") -> ready-for-dev (apply the wei-live-board LIVE pattern to /monitor/fx: GET /api/sym/fx/matrix/live from USD{CCY}=X quotes (probed = currency-per-USD), crosses re-derived, live 1D vs latest-EOD-cross, per-currency freshness, EOD fallback; LIVE/EOD toggle + auto-refresh). Earlier 2026-06-22: indexes->indices standardised project-wide (commit efea397: route /api/sym/indexes->/indices + page URL + gateway method + api-types regen; 22 files; MSCI vendor key/verb/singular-index_* kept). Earlier 2026-06-22: wei-add-regional-indices code-reviewed -> done (2 low patches: STOXX 600 country, ruff-clean). Earlier 2026-06-22: wei-add-regional-indices dev complete -> review (Hang Seng/CSI 300/STOXX 600 added; MSCI EM already present; FTSE pair deferred). Earlier 2026-06-22: story wei-add-regional-indices created (bmad-create-story) -> ready-for-dev (Hang Seng/CSI 300/STOXX 600 to add; MSCI EM already present; FTSE pair deferred). Earlier 2026-06-22: wei-live-board code-reviewed -> done (3 patches: rollup partial-coverage freshness, stuck-spinner, eod_last guard). Earlier 2026-06-22: wei-live-board dev complete -> review (LIVE mode for the WEI board: /indexes/board/live + EOD/LIVE toggle, live re-base + per-index freshness). Earlier 2026-06-22: session retrospective held -> retro-2026-06-22-session.md (board close-out + 2 new stories: non-brazil-gics-surgical-close, indexes-add-vix; lessons: re-measure before scoping, judge long DB steps by the DB not stdout). Earlier 2026-06-22: indexes-add-vix code-reviewed -> done (2 low patches applied, no High/Med). Earlier 2026-06-22: indexes-add-vix dev complete -> review (VIX on the Indexes page via the benchmark registry; category field keeps it off the equity WEI board; honest level-not-return framing). Earlier 2026-06-22: non-brazil-gics-surgical-close -> done (FB spurious ProShares-ETF reversed out of sp500; new `manual` operator-asserted classification source closes the 3 FTSE-100 investment trusts -> Financials; coverage 99.1->99.3%, live-member GICS residual = 0; 838 sym tests green). Earlier 2026-06-22: story indexes-add-vix created (bmad-create-story) -> ready-for-dev (surface VIX on the Indexes page). Earlier 2026-06-22: nasdaq100-universe finished off -> done (re-verified live: 102 members, validate 10/3/1, lone FAIL is pre-existing global unpriced — 0 nasdaq100 members affected). Earlier 2026-06-22: QL-1-lineage-dagster-foundation finished off -> done (AC10/12/13 delivered by QL-2/QL-3, verified + 22 lineage tests green; status-housekeeping close). Earlier 2026-06-22: Monitor arc code-review (bmad-code-review, 3 adversarial layers x 3 groups): the 4 Monitor stories (wei-world-equity-indices, wei-backdate-as-of-date, monitor-fx-cross-matrix, portfolios-live-header-pnl-declutter) reviewed -> done. 4 patches applied (FX ccys dedupe, WEI stale-tooltip honesty, canonical as_of_date SQL placeholder, live-page test-masking fix), 9 deferred to deferred-work.md, ~14 dismissed. 11 api + 27 web tests green, tsc clean.
# 2026-06-20: retrospectively enumerated the standalone / cross-cutting stories (delivered OUTSIDE the
#   epic decompositions) into a new development_status section below (operator: "we can add to status
#   file retrospectively"). These are the inline-tracked extras the DERIVATION NOTE references; statuses
#   derived from each artifact's `Status:` field. Legacy 'review' entries (B-series, ext-company-names,
#   E1) are left as-declared — they may be stale (the work shipped) rather than silently promoted.
# story qh-3-readonly-db-role created 2026-06-13; implemented + reviewed 2026-06-14 (-> done)
# story q8-5-kinea-brazil-macro-feeder created + implemented 2026-06-14 (central-bank feeders
#   BCB/IBGE/Focus/Treasury + sell-side macro display; -> done)
# story qh-4-operate-sse-progress created 2026-06-15 (Operate panel: SSE stream replaces 2s
#   polling; status still derived from heartbeat/pipeline_run_log; -> done, reviewed 2026-06-15)
# story qh-6-generic-module-framework-palette created 2026-06-15 (frontend subnav-provider
#   registry generalizing the bespoke subnavFor + FR-2 command palette; -> done, reviewed 2026-06-15)
# Epic QH retrospective held 2026-06-15 (epic-qh-retro-2026-06-15.md). DECISION: QH.2 is
#   deploy-gated (engine ready; no live quote source reachable in-env), NOT incomplete — Epic QH
#   counts functionally complete (QH.4+QH.6 landed on main fb6ad86). Headline action: console
#   test harness (vitest + @testing-library) for the palette + subnav registry.
project: QRP
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ===== sym Module 1: Identity, Prices, Returns (Epics 1-3) =====
  epic-1: done
  1-1-project-scaffold-and-migration-harness: done
  1-2-currency-and-exchange-reference-tables: done
  1-3-core-identity-schema: done
  1-4-securities-review-queue: done
  1-5-seed-universe-definition: done
  1-6-figi-assignment-via-openfigi: done
  1-7-security-lifecycle-and-filtering: done
  1-8-gics-classification-scd: done
  epic-1-retrospective: optional

  epic-2: done
  2-1-trading-calendar-reference-table: done
  2-2-source-abstraction-contract-and-yfinance-adapter: done
  2-3-raw-price-and-factor-storage: done
  2-4-anomaly-annotation-at-ingestion: done
  2-5-three-phase-load-orchestration: done
  2-6-pipeline-run-logging: done
  2-7-eodhd-adapter-replay-fixtures: done
  2-8-immutability-and-weekly-sweep: done
  2-9-durability-and-disaster-recovery: done
  epic-2-retrospective: optional

  epic-3: done
  3-1-window-math-specification: done
  3-2-adjusted-price-view: done
  3-3-view-performance-spike: done
  3-4-fact-returns-loader-pr-matrix: done
  3-5-total-return-matrix: done
  3-6-incremental-recompute-anomaly-gate: done
  3-7-survivorship-invariant: done
  3-8-returns-accuracy-harness: done
  epic-3-retrospective: optional

  # ===== sym FX Layer (Epic FX) =====
  epic-fx: done
  fx-1-storage-canonical-direction: done
  fx-2-frankfurter-ingest: done
  fx-3a-derivation-view-asof-resolver: done
  fx-3b-conversion-api: done
  fx-4-cli-eod-validation: done
  epic-fx-retrospective: done

  # ===== sym Universe Layer (Epics U1-U5) =====
  epic-u1: done
  u1-1-universe-registry-and-provider: done
  u1-2-membership-event-log: done
  u1-3-resolution-bridge: done
  u1-4-pit-membership-projection: done
  u1-5-asof-query-api-set-ops: done
  u1-6-reproducible-snapshots: done
  u1-7-seed-50-name-universe: done
  epic-u1-retrospective: optional

  epic-u2: done
  u2-1-fmp-index-provider: done
  u2-2-etf-holdings-provider: done
  u2-3-wikipedia-provider-revision-diff: done
  u2-4-source-preference-fallback: done
  u2-5-seed-sp1500-pit-backfill: done
  u2-6-seed-european-flagships: done
  epic-u2-retrospective: optional

  epic-u3: done
  u3-1-daily-maintenance-monitor: done
  u3-2-sanity-gating-corroboration: done
  u3-3-membership-accuracy-gate: done
  u3-4-universe-review-digest: done
  epic-u3-retrospective: optional

  epic-u4: done
  u4-1-drive-ingestion-from-membership: done
  u4-2-historical-backfill-on-join: done
  u4-3-leaver-handling: done
  u4-4-coverage-visibility-scale: done
  epic-u4-retrospective: optional

  epic-u5: done
  u5-1-minimal-fundamentals-input: done
  u5-2-criteria-provider: done
  epic-u5-retrospective: optional

  # ===== sym Cross-Layer Validation (Epic V) =====
  epic-v: done
  v-1-completeness-contract: done
  v-2-referential-integrity: done
  v-3-symbology-name-uniqueness: done
  v-4-price-calendar-lifecycle: done
  v-5-projection-reconciliation: done
  v-6-returns-readiness-gate: done
  v-7-validate-orchestration: done
  epic-v-retrospective: optional

  # ===== QRP Platform v1: Console Spine + sym See/Operate (Epics Q1-Q3) =====
  epic-q1: done
  q1-1-design-foundation-app-chrome: done
  q1-2-config-driven-api-spine: done
  q1-3-branded-module-aware-shell: done
  q1-4-typed-contract-generated-types: done
  epic-q1-retrospective: optional

  epic-q2: done
  q2-1-warehouse-overview-freshness: done
  q2-2-securities-universes-explorer: done
  q2-3-security-detail: done
  q2-4-attention-queue-readonly: done
  q2-5-validation-results: done
  q2-6-universe-heat-map: done
  epic-q2-retrospective: optional

  epic-q3: done
  q3-1-foundation-spike-execution-model: done
  q3-2-run-history: done
  q3-3-trigger-operation-guarded-job: done
  q3-4-run-status-progress: done
  epic-q3-retrospective: optional

  # ===== QRP Roadmap: portfolios / analytics / backtest / optimiser (Epics Q4-Q7) =====
  epic-q4: done
  q4-1-portfolio-weight-store: done
  q4-2-upload-weights-symid-resolution: done
  q4-3-client-entity-context: done
  q4-4-browse-inspect-portfolios: done
  q4-5-weight-history-over-time: done
  epic-q4-retrospective: optional

  epic-q5: done
  q5-1-portfolio-daily-return-series: done
  q5-2-return-pnl-across-windows: done
  q5-3-risk-metrics-vs-benchmark: done
  q5-4-skill-metrics: done
  q5-5-benchmark-selection: done
  epic-q5-retrospective: optional

  epic-q6: done
  q6-1-walk-forward-engine: done
  q6-2-run-config-equity-curve: done
  q6-3-parameterised-strategy: done
  q6-4-output-as-paper-portfolio: done
  epic-q6-retrospective: optional

  epic-q7: done
  q7-1-mean-variance-solver: done
  q7-2-solution-weights-persistence: done
  q7-3-general-objective-constraints: done
  q7-4-output-as-portfolio-backtest-scored: done
  epic-q7-retrospective: optional

  # ===== QRP Roadmap: altdata/macro + signal (Epics Q8-Q9) =====
  epic-q8: done
  q8-1-macro-series-store-ingest: done
  q8-2-altdata-series-store-ingest: done
  q8-3-broaden-altdata-sources: done
  q8-4-broaden-macro-coverage: done
  q8-5-kinea-brazil-macro-feeder: done
  epic-q8-retrospective: optional

  epic-q9: done
  q9-1-factor-catalog-scoring: done
  q9-2-signals-from-macro-altdata: done
  q9-3-input-method-traceability: done
  q9-4-signals-consumable-optimiser-backtest: done
  epic-q9-retrospective: optional

  # ===== QRP Epic QH: Production Hardening (cross-cutting) =====
  epic-qh: done  # QH.1-9 all done. QH.9 (live heatmap) reviewed + 2 patches applied 2026-06-16.
  qh-1-close-brazil-gics-gap: done
  qh-2-live-quote-source: done  # reviewed 2026-06-16: 2 honesty-bug patches applied (freshness rollup, malformed-numeric degradation), 78 tests green; live quotes via Yahoo v8 chart REST (US ~real-time, B3 ~15m); see reference-env-external-sources
  qh-3-readonly-db-role: done
  qh-4-operate-sse-progress: done
  qh-5-migration-finish-off: done
  qh-6-generic-module-framework-palette: done
  qh-7-console-test-harness: done  # 2026-06-16: vitest harness + 17 tests (palette/registry/live-badge); 12-error lint baseline cleared; reviewed + 5 patches applied; npm test 17/17, eslint 0, tsc + next build green
  qh-8-console-fetch-hardening: done  # 2026-06-16: AbortController newest-wins + benchmarks alive guard, palette openRef close-safety + latch split, portfolios error+retry; reviewed + 2 patches (r.ok check, ops array-latch); 25 tests, eslint 0/0, tsc + build green
  qh-9-live-heatmap: done  # 2026-06-16: LIVE mode recolors the treemap by live return (reuse QH.2 quotes + EOD assembly); bounded ThreadPool fan-out + LIVE_HEATMAP_MAX; reviewed + 2 patches (budget actually enforced, badge !loading); 90 api + 27 console tests, eslint 0/0, tsc + build green
  epic-qh-retrospective: done  # held 2026-06-15; augmented 2026-06-16 (after QH.2); 3rd pass 2026-06-16b (epic-qh-retro-2026-06-16b.md) covering the QH.7/QH.8 console-hardening arc + the stop rule

  # ===== Standalone / cross-cutting stories (delivered OUTSIDE the epic decompositions) =====
  # Retrospectively enumerated 2026-06-20 (see the last_updated note). Keys = artifact basename under
  # _bmad-output/implementation-artifacts/. Status from each file's `Status:` field. NOT part of any
  # *epic*.md decomposition (which the development_status entries above are derived from); listed here
  # for visibility only. qh-9-live-heatmap and q8-5-kinea-brazil-macro-feeder are already enumerated
  # in their epic sections above and are not repeated here.

  # -- sym extras (identity / prices / returns) --
  1-9-review-queue-gating: done
  1-10-symbology-scd-transitions: done
  3.1-ext-return-window-expansion: done
  3.2-ext-price-extremes-52w: done   # 2026-06-20: implemented + migrations deployed + live spot-check exact + surfacing follow-up (52W range bar). Code-review (3 adversarial layers): all 8 ACs satisfied, no High/Med; 3 decisions (hash-price patch / gated-stale defer / accept surfacing scope) + 2 patches applied (input_hash now includes price[as_of]; msci-import summary line); 2 defers logged. 37 sym + 14 api + 17 web tests green, 0 new lint.
  ext-company-names: review        # legacy 'review' — may be stale
  E1-eod-orchestration: review     # legacy 'review' — may be stale

  # -- index benchmark spine (B-series) --
  B1-instrument-identity: review   # B1-B6 legacy 'review'; B7 (which builds on them) is done
  B2-index-levels: review
  B3-index-returns-alpha: review
  B4-msci-file-import: review
  B5-universe-benchmark-link: review
  B6-canonical-index-figis: review
  B7-identity-key-bridge: done

  # -- universe extras --
  U1-7-resolution-watermark: done  # distinct from u1-7-seed-50-name-universe (epic-u1)
  U3-5-wire-safety-machinery: done
  U3-6-maintenance-plans: done
  U3-7-provenance-aware-correct: done
  nasdaq100-universe: done  # 2026-06-22 finished off (work + Andre's accept were 2026-06-16/17; never flipped). Re-verified live: 102 current members, validate 10 pass/3 warn/1 fail — lone FAIL is the pre-existing GLOBAL unpriced_securities scan (0 nasdaq100 members affected; universe_readiness PASS). identity_completeness now PASS, classification 99.1% PASS. Residual non-PASS = deferred non-Brazil-GICS (HON) + pre-existing global unpriced, neither this add's doing.
  universes-landing-coverage: done

  # -- lineage / Dagster (QL-series) --
  QL-1-lineage-dagster-foundation: done  # 2026-06-22 finished off: AC1-9 built ad-hoc + AC11 here; AC10 delivered by QL-2 (generate.py→derived_lineage.py), AC12+AC13 by QL-3 (FK_REFERENTIAL merge + diagram.py→docs/field-flow.md). Verified code + 22 lineage tests green; task boxes were just never checked (status-housekeeping per deferred-work 2026-06-10).
  QL-2-auto-feeder-rollout: done
  QL-3-fk-referential-and-visual: done
  QL-4-console-wiring: done
  QL-5-eod-schedule: done
  dagster-job-buckets: done  # 2026-06-23 code-reviewed (bmad-code-review, 3 adversarial layers) -> done. 10 patches applied (fail-fast unknown-subcat + empty-plan-no-longer-silent-green, TimeoutExpired caught for attempt-all, as_of_date validated up front, EOD endpoint never-500s, per-country days_behind clamped >=0, empty-cmd guard, recompute marked critical + date-windowed, honest deferred-selection log, +13 new Part-A tests for AC#8), 3 deferred (universe refresh-mode flag, rates registry discovery, api-types regen), 1 dismissed (null-startTime ordering). No unresolved High/Med. Re-verified: 44 lineage+EOD + 169 api green, ruff clean, 9 jobs load. Earlier: dev complete (autonomous, Andre away) -> review. Built BOTH parts on ONE shared dependency-light taxonomy `lineage/buckets.py` (nine buckets → datasets/date-col/cadence/stale-threshold + job name; no dagster import so the API can consume it). PART B (Data Monitor, built FIRST per Andre): new top-level `data-monitor` rail area (platform.toml + nav DATA_MONITOR_SUBNAV + layout) with an `/data-monitor/eod` page; `EodMonitorGateway` (services/api modules/data_monitor) reads each package DB read-only for actual-vs-expected (expected = latest equity trading session; per-market broadly-complete COVERAGE session for the wide prices table so a fresh sub-universe can't mask laggards; rates broken down per-country with worst-lagging surfaced; classify() with per-bucket stale thresholds) + best-effort Dagster GraphQL run lookup (degrades to none when daemon down) + warehouse-summary header; GET /api/data-monitor/eod resilient (one unreadable dataset -> unknown, never 500s). SUPERSEDES + REMOVES the sym Overview (page + SYM_SUBNAV entry + /api/sym/overview route + overview()/SymOverview/LastRun + test repointed; freshness.py KEPT + reused). PERF: calculations uses plain max (fact_returns coverage-session was 12s/28-windows) + dagster timeout 2s->1s => endpoint 16s->2.5s. PART A (nine bucket jobs): `lineage/bucket_jobs.py` — one config-driven @job per bucket (BucketConfig subcategories[]=all + as_of_date), empty=aggregate command, selection=per-subcategory (universe/country discovered), attempt-all per-subcat, critical=validate; registered in definitions.py alongside sym_eod/rates. (Dagster pythonic-config gotcha: `from __future__ import annotations` breaks list[str] config resolution on factory-defined ops -> dropped it.) VERIFIED: 191 api+lineage tests (incl. 9 new EOD tests) + 841 sym green, ruff clean, definitions load (9 jobs, no collision), /data-monitor/eod renders all 9 buckets (7 ok/2 stale incl. real rates CH-326d), /api/sym/overview 404. RUNNING NOW: `dagster job execute -j sym_eod_job` (sym EOD as_of 2026-06-23) to bring datasets up to date. CAVEAT: web tsc/vitest not runnable locally (page uses LOCAL types, no api-types regen); api-types still carries dead SymOverview entries (regen follow-up). Spec: dagster-job-buckets.md. orig 2026-06-23 created (bmad-create-story, Andre: "create jobs into big buckets: fx, equity prices, index levels, rates, fundamental, alt data, calculations; each broken down further (rates by country, equity by universe); trigger via Dagster; all subcategories by default but selectable to one/multiple for any given date"; +follow-ups: add `universe` (check/refresh constituents) + `macro` split out of alt_data). NINE config-driven bucket jobs in packages/lineage (fx/equity_prices/index_levels/rates/fundamental/alt_data/macro/universe/calculations) generalising the trigger-only sym_eod/rates schedule pattern: a list[str] subcategory selector (empty=all) + as_of_date, attempt-all per-subcat, discovered universe/country lists (universe/equity/fundamental share `sym universe list`; rates from country registry). universe bucket defaults to `monitor` (refresh behind a mode flag); macro needs a thin --source filter on `python -m macro.ingest`. Dagster-only (no web/schema). Recommended config-driven over static partitions (rationale in story). Spec: dagster-job-buckets.md.

  # -- operate hardening (O-series) + durable review surfaces --
  O-2-operate-hardening: done
  O-3-actuation-origin-guard: done
  O-4-error-envelope: done
  S-1-durable-review-surfaces: done

  # -- analytics / console (A / C series) --
  A-1-analytics-boundaries: done
  C-1-console-submenus: done
  C-2-macro-comparison: done

  # -- classification (multi-source GICS) --
  classification-multisource: done
  multi-source-classification-matrix: done
  classification-robustness: done

  # -- explorer enrichment / sym de-hub --
  explorer-enrichment: done
  dehub-sym: done

  # -- fixed income / rates (new asset class) --
  fx-matrix-rates-curves: done  # 2026-06-23 AUTONOMOUS overnight build COMPLETE (commits b58d7c9, 1e68a3c, e270143). 16 countries loaded from central banks (GB prior + DE/EU/FR/IT/ES/US/JP/CH/CA/AU/NZ/SE/NO/HK/BR), all historical; EUR broken down by country (DE full Svensson curve + EU aggregate + FR/IT/ES 10y monthly). `rates validate` GREEN (no FAIL) across all 16 (a few staleness WARNs = real source publish-lag: CH ends 2025-07-31, JP/HK ~monthly, AU 6d). Schema re-keyed on (country,curve_set,basis,rate_type,tenor,as_of_date); rate_type broadened spot/forward/par/yield; band widened; 2 covering indexes (as_of_date- + tenor-leading). 15 source adapters (pure-parse/network split, registry). Country-aware gateway/router + /countries + /curve/compare(/tenor); generic per-country spreads (UK keeps curated breakeven/ASW). /rates page: 16-country switcher + adaptive controls + "Compare across countries" overlay — VERIFIED end-to-end via headless Chrome (GB->DE->US switch, spreads load, 4-country overlay renders, no JS errors). 47 py tests green, ruff clean. PERF: cast spread tenor list to ::numeric[] (psycopg float8[] vs NUMERIC col bypassed the index -> 16s seqscan -> 0.3s); countries() dropped count(DISTINCT) trap. rates_world_daily Dagster schedule added (18:30 America/New_York, explicit tz, STOPPED). NOT loaded (re-test triggers in PULL_REPORT.md): DK (curve discontinued 2012, DE/EU proxy), MX (needs free Banxico token), CN (undocumented POST), SG (auction-only), FR/IT/ES full curves (ECB=10y only). CAVEATS: schema applied via psycopg (Docker down) -> sqitch deploy of multi_country PENDING; RBNZ needed a full browser UA (WAF). Report: packages/rates/PULL_REPORT.md. Spec: fx-matrix-rates-curves.md.
  rates-curve-analytics: done  # 2026-06-22 code-reviewed (3 layers) -> done. Edge "SHIP" 0-High; Auditor 6/7 ACs; Blind's lone "High" (OIS double-fetch) downgraded by read-access layer (abort cancels stale req, backend returns empty not error). 4 patches: added missing rates/layout.tsx tab strip (AC#5 gap); clamp OIS basis in curve fetch (no wasted/empty fetch); renamed `window` state->histWindow (global-shadow); fixed stale checks.py module docstring (fwd-spot now exact FAIL not WARN). 3 deferred (full-history-read perf watch-item, leg-calendar thinning, window-floor anchoring), rest dismissed. Post-patch: ruff clean, 39 py tests, /rates HTTP 200 + dump-dom tab-strip+shell render no error overlay. CAVEATS unchanged: web tsc/eslint/vitest not runnable locally (CDP-verified); api-types regen + LIVE page->API end-to-end pending :8001 restart (running server predates rates install -> /api/rates 404s until bounced); only latest-bundle loaded (15 days/13,140 nodes, 2026-06-01..19), full-history archive backfill deferred. orig 2026-06-22 dev complete -> review. Derive-on-read analytics over rates.curve_point (persist nothing; store untouched). Task 1: pinned BoE conventions from the FAQ in-env (continuously compounded/annual; gilt ACT/ACT else ACT/365) -> DF=exp(-s*t); upgraded check_forward_spot_reconcile WARN->FAIL exact continuous-compounding identity (0 fail real data). Task 2: pure analytics.py (interp/discount/zero, 2s10s/2s5s10s-fly/breakeven-%/ASW-proxy-bp, roll/carry off forward curve, DV01/PV over arbitrary cashflow) +10 hand-computed tests. Task 3: spreads()+spread_history() gateway (one query per leg-group, z-score/pctile via statistics.mean/pstdev, N/A-not-zero when leg unpublished) -- real data: 2s10s 75.4bp z+0.39 73pct, fly -33bp, 10y BE 3.19%, ASW 48bp. Task 4: GET /api/rates/spreads + /spread/{key}?window. Task 5: app/rates/page.tsx (curve chart spot/forward x nominal/real/inflation+OIS, spread monitor cards w/ z-score+sparkline + date-axis history drill-in, two-tier density, RPI-not-CPI/ASW-proxy labelled) + RATES_SUBNAV/registry in nav.ts (rail link auto-resolves from platform.toml). Verify: ruff clean, 39 py tests (no regression: store's 23 + 10 analytics + 6 spreads/router), validate exit 0, curve coverage intact, /rates HTTP 200 + headless dump-dom shell render no error overlay; rates-page.test.tsx written. CAVEATS (deferred-work): web tsc/eslint/vitest NOT runnable locally (apps/web node_modules incomplete - empty .bin, no top-level ts/eslint/vitest/next; reinstall = churn-forbidden per feedback_minimize_dev_churn -> CDP-verified instead, sanctioned method); api-types regen + live page->API end-to-end pending :8001 restart (page uses LOCAL types not Schemas, non-blocking); full carry monitor card a small follow-up. orig 2026-06-22 dev-story started. created (bmad-create-story, Andre: "kick off" the derived-analytics follow-on to uk-rates-curve-store 0c48904). The trading layer the brainstorm + store deferred: DERIVE-ON-READ over rates.curve_point (persist nothing). Scope: pin BoE compounding/day-count from the methodology doc + upgrade check_forward_spot_reconcile from approximate WARN to EXACT (gate) -> pure derive module analytics.py (discount/zero rate + interpolation, spreads 2s10s/general-N-s-M/2s5s10s-fly/breakeven=nominal-real/asset-swap-proxy=gilt-OIS, carry & roll-down off the forward curve, DV01/PV over an ARBITRARY cashflow list) -> history + z-score/percentile gateway (reuse signals/compute.py mean/pstdev + macro one-pass enrichment; N/A-not-zero when a leg tenor unpublished) -> read API GET /api/rates/spreads + /spread/{key} -> rates console page (apps/web/app/rates/ curve chart spot/forward x basis + spread monitors w/ z-score sparklines, date-axis.ts, RATES_SUBNAV in nav.ts, two-tier density, EOD-only). Honesty: breakeven=RPI-not-CPI, asset-swap=labelled proxy until bond static data. OUT (still deferred): bond reference-data (specific-gilt cashflows = curve->position bridge), live mark, 2nd-source divergence, multi-country. Spec: rates-curve-analytics.md + brainstorming-session-2026-06-22-211134.md. Open Qs: split API-vs-page? spread set? carry horizon? asset-swap proxy ok? (defaults chosen, non-blocking).

  uk-rates-curve-store: done  # 2026-06-22 code-reviewed (3 adversarial layers) -> done. Auditor 5/9 ACs fully met; no data-corrupting bug (Blind Hunter's lone "High" xmax=0-classification REFUTED — macro/ingest.py uses the identical idiom). 1 decision RESOLVED (keep single-row two-vintage model, AC#3 reconciled). 7 patches applied: rates added to services/api deps+uv sources (lone enabled router outside the dep closure); rates registered in tools/deploy_all.py (DB deploys via house method); close read-only openpyxl workbook (handle leak); decouple tail desync gate from --archive (fill_curve tail=not archive); missing-tenor hole guard in check_plausible_band (+test); .pytest_cache/.ruff_cache gitignored+removed; fixed tautological ingest test. 8 deferred (deferred-work.md), 6 dismissed. Post-patch: ruff clean, 23/23 tests, live validate exit 0. orig 2026-06-22 dev complete -> review. Built the NEW `rates` package end-to-end (peer to sym/macro, own `rates` DB): db/cli/ingest/validate/sources(boe)/gateway/router + sqitch curve_point trio + 22 DB-free tests. PROBE (Task 1) reachable in-env + corrected the schema: BoE publishes SPOT+FORWARD, NO par (free check became inflation=nominal-real exact + forward->spot approximate WARN); curve sets GLC (gilt nominal 0.5-40y, real/inflation 2.5-40y) + OIS (nominal 0.5-25y); two tenor-grid resolutions deduped; values % p.a. (real negative allowed). curve_point: (curve_set,basis,rate_type,tenor,as_of_date) PK, two vintages in one row (first_value immutable PIT + value restated latest, last_changed_at re-stamps only on change), source-tagged, plausibility>5pp -> curve_point_review, atomic per-day, tail-case desync gate. Created `rates` DB + applied schema directly (Docker DOWN -> sqitch deploy pending, IF NOT EXISTS). LOADED REAL BoE DATA: 13,140 nodes/8 series/15 days (06-01..06-19; 06-22 not yet published = honest EOD lag), idempotent re-load (0 ins/13140 skip). `rates validate` ALL 4 PASS exit 0 (inflation=nominal-real within 0.02pp over 102 nodes = the free check; band 438; fwd->spot within 0.5pp/125). Router+gateway verified in-process (`GET /api/rates/curve` + `/curve/series`, as-of + first-vintage reads). pytest 22/22, ruff clean. Wiring: pyproject workspace member + platform.toml module (enabled) + main.py mount + lineage `rates_curve_daily` schedule (Europe/London explicit tz, STOPPED). CAVEATS (deferred-work): running API :8001 predates rates install -> serves /api/rates only after restart (verified in-process, not force-restarting per minimize-churn); api-types regen deferred to follow-on console story (no web consumer yet); full-history archive backfill (~39MB zips) not run (latest bundle only). Memories: project_rates_package_decision + project_fi_curves_brainstorm. orig 2026-06-22 created (bmad-create-story, after FI-curves brainstorm) -> ready-for-dev. v1 of the fixed-income direction: a trustworthy PIT UK rates curve store. TOPOLOGY decided by Andre (create-story AskUserQuestion): a NEW `rates` package, peer to sym/macro, modeled on macro — NOT inside sym (honors per-package/de-hub direction; FI is its own bounded context). Source = Bank of England daily published yield curves (gilt nominal/real/implied-inflation + SONIA/OIS), OGL-licensed Excel/zip. Scope = brainstorm steps 1-4: probe BoE+methodology+maintenance-plan (GATE) -> scaffold `rates` -> `curve_point` grid schema (curve_set/basis/rate_type/tenor/as_of_date/vintage; immutable, source-tagged, two vintages first-published+latest, tenor-as-data, canonical as_of_date) -> BoE ingest (fill_curve mirroring fill_fx; atomic per-day all-4-bases, parse-layout-assert, plausibility gate) -> validate (reconciliation par/forward<->spot FREE CHECK, inflation=nominal-real RPI-not-CPI, plausible-band, stale-gating vs UK calendar) -> London-EOD schedule (explicit timezone) -> minimal GET /api/rates/curve. Store BoE's PUBLISHED FITTED grid verbatim + derive-on-read (atom = curve grid, not raw gilts). Explicit follow-ons (OUT of scope): derived analytics (spreads 2s10s/flies/breakeven/asset-swap + carry/roll/DV01 + console page), bond reference-data (specific-gilt cashflows = curve->position bridge), live mark, 2nd-source divergence, multi-country. Spec: uk-rates-curve-store.md + _bmad-output/brainstorming/brainstorming-session-2026-06-22-211134.md.

  # -- commodities (new asset class) --
  commodities-store: done  # 2026-06-24 code-reviewed (bmad-code-review, 3 adversarial layers) -> done. NEW `commodities` peer package (Tier-A yfinance =F continuous front-month) + /commodities monitor page, built autonomously from the 2026-06-23 brainstorm v1 spec. 6 patches applied (OHLCV restate guard; Sqitch trio + deploy_all; Dagster commodities_daily schedule explicit-tz STOPPED; yfinance kept-0 warn; CLI band/conn cleanup; page decimal/as-of), 6 deferred, ~9 dismissed; no unresolved High/Med. 9 commodities tests green, ruff clean, lineage.definitions loads. CAVEAT: web toolchain not runnable locally (inspection-reviewed); api-types regen + live page->API pending :8001 restart. Spec: commodities-store.md + brainstorming-session-2026-06-23-224317.md.
  # -- simulations credibility (backtest / optimiser) --
  next-level-simulations-tier1: done  # 2026-06-24 Tier-1 COMPLETE. 1A txn-costs/turnover + 1B-step1 spread-t-stat + 1C Ledoit-Wolf shrinkage shipped 2026-06-23 (89b0f6f/4291e1f, defaults confirmed 2026-06-24). Tier-1 REMAINDER (DSR + PBO/CSCV + MinBTL + backtest.sweep infra) code-reviewed (3 adversarial layers) -> done: backtest/stats.py + sweep.py + sweep.sql migration + engine return_daily + gateway/router (POST /api/backtest/sweep, GET /sweeps[/{id}]). Auditor all 6 spec items MET; 2 patches (run_sweep attempt-all, PBO tie-aware avg rank), 6 deferred, 6 dismissed; 39 backtest tests green, ruff clean. FOLLOW-UPS CLOSED 2026-06-24: (a) backtest.sweep + commodities.price_daily schemas deployed+verified via Docker sqitch (deploy_all, both up to date); (b) /backtest page Overfitting-sweep panel shipped — grid builder (top_pct x rebalance) + sweeps list + DSR/PBO/MinBTL verdict cards + credible badge + best-config run link (apps/web/app/backtest/page.tsx; local TS types pending api-types regen; verified by inspection + live GET /api/backtest/sweeps returning a real 6-config verdict, web toolchain not runnable locally). Spec: next-level-simulations.md.
  # -- fixed income / rates: additional sources (2nd-source / richer US) --
  rates-sources: done  # 2026-06-24 code-reviewed (Blind + Edge; no-spec) -> done. NEW US Fed GSW fitted-curve source (nominal spot/par/forward + TIPS real + breakeven inflation, curve_set='gsw') alongside the Treasury CMT par curve; ECB euro-area source expanded to govt + govt_all universes x spot/forward/par x 12 tenors; build_registry() -> dict[str,list[CurveSource]] (US has two sources); source provenance surfaced through gateway/router/page; cross-country overlay moved onto the main curve chart + sqrt-tenor interpolation fix. 1 patch (ECB per-series parse/decode tolerance + wholesale-outage raise, +2 tests), 3 deferred, 3 dismissed. 57 rates tests green, ruff clean. CAVEAT: web reviewed by inspection (toolchain not runnable locally); api-types regen + live verification pending :8001 restart. The fx-matrix-rates-curves "2nd-source divergence / richer US" follow-on. Spec: none (no-spec build); context fx-matrix-rates-curves.md.

  # -- console polish --
  menu-title-case: done  # 2026-06-24 code-reviewed (bmad-code-review, 3 lenses inline — proportionate to a 24-line config label edit) -> done. Clean: 0 decision/0 patch/0 defer/1 dismiss (CRLF raw-diff churn — git normalizes; staged diff = exactly 12 ins/12 del, 0 key changes), no High/Med, AC1-5 all met. Earlier: dev complete (bmad-dev-story) -> review: platform.toml 12 module `name` fields Title-Cased (sym->Sym per Andre's choice 2; Data Monitor already cased; QRP untouched); keys/routes/descriptions unchanged; no code logic (sidebar renders m.name verbatim). Verified: config.enabled_modules() + live GET /api/platform (API restarted) return all Title-Case; no test asserts old labels (sidebar/palette fixtures already Title-Case). orig 2026-06-24 created (bmad-create-story, Andre: "Make the menu Camel case monitor -> Monitor, alt data -> Alt Data"). Title-Case the left-rail module labels via platform.toml [[modules]] name fields ONLY (monitor->Monitor, alt data->Alt Data, macro/rates/commodities/signals/backtest/optimiser[keep -ise]/portfolios/analytics/lineage; Data Monitor already cased). Decision resolved (Andre chose 2 = full uniformity): sym->Sym too (display-only; package/DB/key stay `sym`). Keys/routes UNCHANGED. Guardrail: edit explicit strings, NOT a blanket capitalize transform (would mangle FX/QRP-style acronyms). name verified display-only (not a lookup key). Trivial copy change -> no CDP; verify rail labels on :3001. Spec: menu-title-case.md.

  # -- package topology --
  universe-package: review  # 2026-06-25 dev COMPLETE (bmad-dev-story) -> review. ALL of P1-P4 done + committed (72a8032,184cfb0,5b119f2,ee50fa1 on feat/universe-package): own `universe` DB; circular dep inverted (Resolver protocol; SymResolver adapter in sym); 7 tables migrated + DROPPED from sym (universe_benchmark + universe_member_completeness stay, FK->soft ref); ALL consumers cross-DB via roster-fetch (cli/eod/ingest/validate[4 checks + referential]/backtest/signals/api gateway[coverage+heatmap roster-fetch]/data_monitor/indices/fundamentals). sym_contract relations -> UNIVERSE_RELATIONS. VERIFIED live (tables only in universe DB): backtest 501 members, gateway coverage+633-cell heatmap, sym validate all universe checks cross-DB (no crashes; 2 remaining fails are pre-existing data gaps), deploy_all --status clean. Suites: universe 153 + sym 641 + backtest 39 + signals 14 + lineage/api 215; ruff clean; import-guard clean. Next: bmad-code-review (ideally a different model). ORIG 2026-06-24 dev (bmad-dev-story) — P1/P2/P3 DONE + committed (72a8032, 184cfb0 on feat/universe-package); P4 (read-only-consumer cutover + drop-from-sym) REMAINS. P2 inverted the circular dep (universe sym-free + Resolver protocol; SymResolver adapter in sym; monitor/refresh/accuracy/gating injected). P3 migrated the 7 tables sym->universe (counts verified) + rewired the CORE consumers cross-DB via roster-fetch (sym cli universe subcommands, eod monitor step, ingest price-load bridge, backtest/signals _members). Suites GREEN: universe 153 + sym 641 + backtest 39 + signals 14 + lineage/api 215; ruff clean; universe import-guard clean; topology contract updated. P4 REMAINING: rewire validate/{completeness,projection,readiness,plans} (EOD gate) + api gateway (~8 universe reads) + data_monitor universe bucket + indices/links + fundamentals.resolved_member_figis to the universe DB, then drop the 7 tables from sym (universe_benchmark stays; universe_id FK->soft ref). Until P4 those read-only consumers read sym's still-present tables (system works; AC#3 not yet met). ORIG P1 DONE + committed (72a8032 on feat/universe-package): scaffolded packages/universe + its OWN `universe` database (Sqitch universe_schema migration recreating the 7 moved tables in schema `universe`; registered in deploy_all + workspace; created+deployed+verified). ADDITIVE — sym unchanged, zero behavior change. Coupling fully mapped (Explore) + refined design captured in the story Dev Agent Record (Resolver protocol surface, exact move-vs-stay split, cross-DB consumer list). REMAINING P2-P4 (the bulk — bigger than fx, can't be partially committed): P2 invert the circular dep (move ~18 membership modules; resolver IMPL/make_*_resolve_fn + ingest price-load bridge + fundamentals STAY in sym as the injected adapter), P3 migrate the 7 tables + rewire consumers cross-DB via roster-fetch, P4 drop from sym (universe_benchmark stays; universe_id FK->soft ref) + verify. Load-bearing for EOD/backtest/signals — best resumed as a dedicated session. ORIGINAL 2026-06-24 created (bmad-create-story, Andre: "feels like universe should be a separate package" -> "do it" -> "change also to separate database"). FINAL DECISION: approach B = full peer package WITH its own `universe` database (DB-per-package + federation topology), like rates/commodities. Extract the universe/membership subsystem (24 sym/universe/* modules + 7 tables [universe_benchmark STAYS in sym — FKs instrument/sym_id] + the universe CLI) out of sym. TWO KNOTS: (1) circular dep -> dependency INVERSION (universe imports nothing from sym + defines a Resolver protocol; sym provides+injects it; one-way sym->universe). (2) DB split -> universe_* tables LEAVE the sym DB so every operational membership read (sym load, EOD monitor, backtest, signals, validate, data_monitor) is cross-DB; mechanism = ROSTER-FETCH (fetch the small ~hundreds-figi roster from the universe DB, filter sym data by it) NOT a giant cross-DB join / not DuckDB federation. universe is the FIRST package joined operationally with sym. GUARDRAILS: universe_reload_no_gaps (per-member end-caps incl leavers, now sourced from universe DB), identity bridge whole in sym, SCD/projection, no cross-DB FKs, Docker sqitch deploy of the new universe DB. Large phased refactor; adversarial review of dep-direction + behavior preservation + roster pattern. Spec: universe-package.md. SIBLING: fx-package.
  fx-package: done  # 2026-06-24 code-reviewed (bmad-code-review, 3 adversarial layers) -> done. ONE real defect found + FIXED: the fx migration created fx.currency but never seeded it, so a FRESH deploy_all would build a non-functional fx DB (fx_rate FK rejects all inserts, _default_currencies empty) — added the idempotent fx:seed_currency migration (deployed+verified; no-op on the already-seeded live DB). 6 defers logged to deferred-work.md (fx_extract copy-guard, fx<->sym currency drift check, fx-matrix 503-on-DB-down, data-monitor fx degrade test, cli unconditional fx conn, temp-table fixed name) — all ops-hardening/enhancements, none block. 8 dismissed as verified noise (notably the "source-precedence regression" — the ORIGINAL recompute LATERAL also lacked fx_source_rank, so the temp-table version is identical; + several pre-existing/ house-pattern items). Post-fix: deploy_all --status clean (fx+sym up to date), fx suite green. Earlier 2026-06-24 dev complete (bmad-dev-story) -> review. Built packages/fx with its own `fx` database (schema `fx`: currency+fx_rate+fx_rate_review+v_fx/v_fx_daily+fx_source_rank), moved the 9-module FX engine out of sym (imports nothing from sym — import-guard clean), rewired EVERY consumer cross-DB: marketcap/restate/coverage take an explicit fx_conn; recompute_market_cap_usd uses a sym-side TEMP table (identical LATERAL semantics, re-run=0 rows changed); eod fx step + sym fx CLI + sym fundamentals open an fx conn; API gateway fx_matrix/fx_matrix_live read the fx DB via an injected fx_conn (route opens both); lineage fx bucket package=fx; new standalone fx.cli (load/review/divergence/convert). rates needed NO rewire (docstring-only ref). Migrated 388,075 fx_rate + 3 review + 32 currency rows sym->fx (counts verified). Dropped the fx objects from the sym DB via sym:fx_extract migration (currency STAYS — 4 tables FK it; revert recreates; 6 stale fx-create verify scripts no-op'd). 7 documented deviations from the literal story (restate/validate STAY in sym as consumers; sym fx CLI retained behavior-identical; etc.) all serve AC#2 + behavior preservation. VERIFIED: fx 47 + sym 794 + lineage/api 215 suites green, ruff clean, deploy_all --status clean (fx+sym up to date), lineage.definitions loads, live cross-DB matrix all OK. Spec: fx-package.md. Next: bmad-code-review (ideally a different model). 2026-06-24 created (bmad-create-story, Andre: "I want to do that also for fx, so factor that in" — sibling to universe-package). Same destination: full peer package WITH its own `fx` database (DB-per-package topology) like rates/commodities. Extract the FX subsystem (10 sym/fx/* modules + validate/fx.py + the fx CLI) out of sym. SIMPLER than universe — Explore map (2026-06-24): NO inbound FKs to fx_rate + fx is READ-ONLY from sym (restate.py reads prices/securities/fact_returns, never writes), so NO circular dep and NO dependency-inversion knot (contrast universe's Resolver). fx's only hard parts: (1) the fx_rate->currency FK across a DB boundary -> SEED a small `currency` reference table in the fx DB (reference data, dup is fine) keeping the FK intact same-DB; (2) route the thin read-resolvers (fx_rate()/convert()) at the fx DB + turn the ONE set-based recompute_market_cap_usd LEFT JOIN into a small RATE-MAP fetch (~28 currency->usd_rate rows applied locally), NOT a cross-DB join. restate.py moves with fx + takes an explicit (sym_conn, fx_conn) so fx stays sym-import-free. Move objects: fx_rate, fx_rate_review, v_fx, v_fx_daily, fx_source_rank() -> fx DB; NOTHING fx stays in sym (fundamentals.market_cap_usd is a sym column fed by the rate-map). Consumers to rewire at the fx DB: marketcap, fundamentals recompute, eod fx step, validate coverage, api fx_matrix_eod/live, rates ingest, lineage fx_load bucket + fx_rate asset. GUARDRAILS: fx_rate immutability + plausibility-band->review queue + source precedence, USD-base canonical + triangulate, as_of_date canonical name, no cross-DB FKs, Docker sqitch. Lower-risk than universe — good to do first; shares cross-cutting wiring (deploy_all, root+api pyproject, lineage) so sequence to avoid churn. Adversarial review of one-way sym->fx + behavior preservation + currency-FK + rate-map-not-join. Spec: fx-package.md.

  # -- data monitor --
  data-monitor-compact-counts: done  # 2026-06-24 code-reviewed (bmad-code-review, 3 adversarial layers) -> done. Auditor AC1-7 all met (AC6 single-day->trailing-window deviation acceptable: bounded/index-cheap/no-full-distinct/never-500). 3 patches: dead link sym/page.tsx /data-monitor/eod->/data-monitor (the route was deleted), stale "single-day" comments->"trailing window" (false API contract), honesty note+coverage moved to label tooltip (inline re-broke 1366 no-scroll, CDP-confirmed) + null-label space. 1 defer (rates composite-count column coupling), 3 dismiss (SQL f-string frozen-constants, SUBNAV=[] handled, nav.ts CRLF normalizes). Post-patch: ruff clean, 13 data_monitor tests, CDP no-scroll @1440x900 & 1366x768 re-verified. Earlier: dev complete (bmad-dev-story) -> review. Data Monitor de-tabbed (board at /data-monitor, eod/ route removed, layout tab strip dropped, DATA_MONITOR_SUBNAV=[]) + per-bucket TRAILING-WINDOW instrument counts (fx 28 pairs, rates 36 curves, macro 64 series, commodities 22; windowed not single-day because lagged/slow series don't all print on the latest date — single-day read "1 curve"; bounded+indexed; calculations/universe no id_column = no count) + NEW commodities bucket (wide, commodity_code) collision-guarded out of generated BUCKET_JOBS (schedules.py commodities job owns the name) + two-tier compaction. Verified: 48 data_monitor+lineage (+4 new) + 208 api+lineage green, ruff clean, definitions load; API restarted + api-types regenerated; CDP @1440x900 AND 1366x768 = no vertical scroll, all 10 buckets, no EOD tab, counts render. Spec: data-monitor-compact-counts.md. orig 2026-06-24 created (bmad-create-story, Andre 3 asks): (1) compact the Data Monitor EOD board so most buckets fit with no vertical scroll (two-tier density; move run-chips off the collapsed row); (2) add a single-day per-bucket instrument count for the types that lack one (fx pairs, rates curves, fundamentals/macro/alt_data series — via Dataset.id_column generalised beyond `wide`) AND add a NEW `commodities` bucket (commodities.price_daily, wide, id=commodity_code) reading the deployed commodities DB; (3) remove the single EOD tab — board renders at /data-monitor, no tab strip. CRITICAL guardrail: a commodities bucket collides with the existing `commodities` Dagster job (schedules.py) since bucket_jobs.py mints one job per bucket -> exclude commodities from generated BUCKET_JOBS. Perf guardrail: counts MUST be single-day-scoped (no full-table count(DISTINCT); calculations 12s trap). Endpoint must still never-500. Verify via headless Chrome CDP @1440x900 + 1366x768 (web toolchain not runnable locally; dev server :3001, API :8001); regen api-types after the model change. Spec: data-monitor-compact-counts.md.

  # -- portfolio live cockpit --
  portfolios-exposure-and-layout: done
  portfolios-live-heatmap-and-pizza: done
  portfolios-live-grid-eod-returns: done  # created + implemented + reviewed 2026-06-20 (live-rebased trailing returns in the grid; 2 review patches applied)
  portfolio-returns-skip-gated: done  # 2026-06-22 code-reviewed (3 layers) -> done. Clean: all ACs pass, no High/Med; Edge Case Hunter verified end-to-end (all-gated ret_date=None guard exists -> coherent empty payload; no covered_w/n_with_return miscount; only fact_returns date-pin in the gateway; matches the analytics pr-IS-NOT-NULL convention). Blind Hunter's "High" (missing all-gated guard) was a false positive (truncated-diff artifact). 0 patches; 1 defer (behavioral test needs a SQL-capable fake conn) -> deferred-work.md. orig 2026-06-22 created + dev complete -> review (Andre: "portfolio live top-10 YTD not populated"). 3rd surface of the AR-9 gating issue: /api/portfolios/{id}/returns (Top-Movers MTD/YTD source) pinned to the latest date with a fact_returns ROW, counting gated null-pr rows -> pinned to all-gated 06-18 -> 97/100 constituents null -> movers collapse to 3 names. Fix: `pr IS NOT NULL` in BOTH the broadly-complete-date pin CTE + the pr lookup -> pins to the last real-returns date (06-17). Verified live: MTD+YTD now 100/100 (was 3/100). +1 SQL-guard test; API 166 green; ruff clean. Read-only, no frontend change. Mirrors portfolios-live-returns-fix's skip-null rule.
  ticker-region-detail-pivot: done  # 2026-06-22 code-reviewed (3 layers) -> done. Clean: all 5 ACs pass, no High/Med; both read-access layers verified the column-alignment chain (composition 15-col tuple/_MISSING/unpack/dict + detail 4-tuple None-guarded) correct. 0 patches; 1 defer (per-row store subscription in the pivot Ticker column — idiomatic island pattern, immaterial at realistic book sizes) -> deferred-work.md. orig 2026-06-22 created + dev complete -> review (Andre: "do the detail/pivot fast-follow"; the ticker-region-codes Open Q#3 deferral). Extended the qualified ticker to the click-through path: a SHARED convention store (lib/ticker-convention.ts, default Bloomberg Region) replaces the Explorer's local one + a reusable QualifiedTicker client island; detail page h1 + portfolio pivot Ticker column now show the qualified ticker; detail (security_detail) + analytics composition APIs carry exch_code/bbg_exchange_code (+ country_iso) via the existing exchange joins; CompositionHolding/SecurityDetail models + api-types updated. No migration (reuses ticker-region-codes). API 165 + web 150 green; ruff/tsc/eslint clean; CDP: detail "ADS GR · ADIDAS AG" + pivot "SNDK US"/"LITE US". Composition meta unpack widened 12->15 (test _SymConn right-pads; detail country fixtures -> 4-tuple).
  ticker-region-codes: done  # 2026-06-22 code-reviewed (3 layers) -> done. All ACs pass (AC#7d gap closed). 3 patches applied: validate stored convention (no blank select), trim codes in qualifiedTicker (no "ADS "/"ADS-"), +4-test Explorer render suite. Blind Hunter's "High" hydration finding was a FALSE POSITIVE (useSyncExternalStore uses getServerSnapshot during hydration; mirrors FX/theme stores) + "FactSet not ISO-2" false (country_iso CHECK exists). 4 defers (venue-code accuracy + 4 NULLs; detail/attention click-through; verify-missing-row; revert IF EXISTS). web 150 green; ruff/tsc/eslint clean. orig 2026-06-22 dev complete -> review (default = Bloomberg Region per Andre). Investigation: exch_code already = Bloomberg region (GR/US), country_iso already = FactSet region (DE/US); only added bbg_exchange_code (venue GY/UN/UW). sqitch trio + plan entry; seeded 31/35 (4 NULL by design: XASE/ARCX/XSHG/XSHE — flagged for Andre to confirm; null-safe fallback). Applied to dev DB directly (Docker down; sqitch deploy is a no-op pending Docker, ADD COLUMN IF NOT EXISTS). New apps/web/lib/ticker.ts (3 conventions, null-safe) + 6 tests; securities API exposes the codes (+ SecurityRow + api-types regen); Explorer persisted convention selector (default Bloomberg Region) + qualified-ticker cell + all-three tooltip, bare ticker still searchable. Scoped to Explorer; pivot/detail/composition deferred (Open Q#3). API 165 + web 145 green; ruff/tsc/eslint clean. CDP: ADS -> "ADS GR" default, selector -> ADS GY/ADS-DE/ADS. 2026-06-22 created (bmad-create-story, Andre: "show ticker with exchange + region acronym; Bloomberg/FactSet ticker-region; ADS GY (Xetra)/ADS GR (Germany) vs ADS UN/ADS US for Bread Financial"). Investigation: exchange.exch_code ALREADY = the Bloomberg composite/region code (XETR->GR, XNYS->US, reuse it; also feeds OpenFIGI — don't overwrite); exchange.country_iso ALREADY = the FactSet region (DE/US, reuse). ONLY gap = the Bloomberg VENUE code (GY/UN/UW; the US split UN-NYSE vs UW-Nasdaq is the key disambiguator since both are composite US). Plan: add bbg_exchange_code to exchange (sqitch deploy/revert/verify mirroring exchange_figi_exch_code.sql, seed 35 MICs) + a derived qualified-ticker helper (3 conventions: "{tkr} {bbg_exchange_code}" / "{tkr} {exch_code}" / "{tkr}-{country_iso}") + expose via the existing exchange joins (securities/detail/composition) + a persisted convention selector on Explorer/pivot/detail. Derived not stored; null-safe bare-ticker fallback; search still matches bare ticker.
  portfolios-live-returns-fix: done  # 2026-06-22 code-reviewed (3 adversarial layers) -> done. Auditor: all 6 ACs + conventions PASS, no High/Med. Blind Hunter's 2 High/Med findings were FALSE POSITIVES (Daily-P&L divergence + sector double-count) — refuted by the read-access layer: PnlAccess already special-cases DAILY->live_return, and all aggregation uses PnlAccess not windowReturn. 0 patches; 2 minor defers (sort-test strengthening, per-cell trailing-window staleness mark) -> deferred-work.md. orig 2026-06-22 dev complete -> review. 2026-06-22 dev complete -> review. A (console): windowReturn accessor -> "1D Chg" shows live_return (live, 100/100, ticks) not window_returns["1D"]. B (API): composition window query "AND fr.pr IS NOT NULL" -> trailing windows fall back past a null/gated latest row (3->100/100). C REFRAMED — NOT a bug: 06-18 nulls are AR-9 GATED rows (97/100 carry an unreviewed prices_review flag; prices_raw+adj_close fully present), returns withheld by design; sym recompute (2172 secs, 201,768 rows) correctly kept them null; B is the right handling; operator review of the prices_review queue is the out-of-scope follow-up (do NOT auto-clear quality flags). API 165 + web 139 green; ruff/tsc/eslint clean. CDP /portfolios/3/live: 1D Chg 101/101 (was 3), ticks 39/101, 1M 101/101. orig 2026-06-22 created  # 2026-06-22 created (bmad-create-story, Andre: "1D Chg not moving for most of the grid, only 3 names" on /portfolios/3/live). Investigation (grid -> composition API -> fact_returns DB) found THREE causes: (A) console — the grid's "1D Chg" column reads window_returns["1D"] (EOD) instead of the live live_return (Daily P&L already special-cases DAILY->live_return; mirror it); (B) API — composition() picks the latest fact_returns row even when pr IS NULL (verified portfolio 3: 3/100 non-null with current pick vs 100/100 with a non-null skip); (C) data — fact_returns for 2026-06-18 has null pr for 97/100 names (partial-EOD-style break; 06-17 clean), repair via sym recompute (+ bounded sym load --overwrite if prices short). Fix all three; B self-heals the grid by falling back to the last clean date. 3/100 -> 100/100.
  portfolios-live-autorefresh-parity: review  # 2026-06-22 dev complete -> review. Ported the autoSec/refreshedAt/useOnline trio onto the /portfolios/[id]/live cockpit (auto-refresh effect bumps the existing nonce; refreshed HH:MM:SS appended to the FRESH_STYLE badge; auto … s control beside ↻). No EOD/LIVE toggle, kept the pill badge. 138 web green (5 in portfolio-live.test.tsx); tsc/eslint clean; real-Chrome CDP /portfolios/5/live: auto=3s -> "every 3s", refreshed stamp advanced over ~2 ticks. orig 2026-06-22 created  # 2026-06-22 created (bmad-create-story, Andre: "review the portfolio live page so it's like wei and fx pages"). Bring the portfolio live cockpit (/portfolios/[id]/live) to auto-refresh parity with WEI/FX: add autoSec interval control (3s floor, off by default, useOnline-paused) + a "refreshed HH:MM:SS" badge stamp, mirroring heatmap-view/WEI/FX (the cockpit only had a manual ↻). Keep the manual ↻ + FRESH_STYLE pill badge (already matches heatmap-view); NO EOD/LIVE toggle (cockpit is inherently live; static view is /portfolios/[id]); do NOT switch to LIVE_TONE. Target = the cockpit, NOT the /monitor/portfolio-live launcher (a plain list, left as-is).
  portfolios-live-header-pnl-declutter: done  # 2026-06-22 code-reviewed -> done. Fit-to-viewport live cockpit (P&L into header, drop risk/exposure panel, tighten spacing, flatter heat map). Review patch: portfolio-live test getAllByText length>0 was satisfied by the grid column alone (didn't prove the header strip rendered) -> data-testid + within(). 3 deferred (header truncate, strip null-placeholder test coverage).
  portfolios-multi-column-sort: done  # created + implemented + reviewed 2026-06-20 (Ctrl/Cmd-click adds/toggles secondary sort keys; ordered Sort[] + looped compareHoldings + priority indicators; within-sector grouping preserved). Code-review: 8/8 ACs, no High/Med; 1 decision (keep fresh-single-sort) + 2 patches (indicator separator, test assertion). 6 pivot + 20 portfolio tests green, tsc+eslint clean.
  portfolios-donut-container-fit: done  # created + implemented + reviewed 2026-06-20 (donut sized by CONTAINER via Tailwind v4 container queries, not viewport; CSS-gated minor labels; matchMedia hook removed). Real-Chrome verified rail-toggle holds card height. Code-review: all ACs met, 2 patches (drop @sm grow step, harden label test). 31 portfolio tests green.
  portfolios-donut-responsive: done  # 2026-06-20 implemented then SUPERSEDED-and-folded into portfolios-donut-container-fit (viewport->container queries); intent (big donut + more labels on large screens) preserved in the final code. Committed together.
  monitor-fx-cross-matrix: done  # 2026-06-22 code-reviewed -> done. Review patch: fx_matrix didn't dedupe ccys (?currencies=USD,EUR,EUR -> dup grid rows/cols + dup React keys) -> list(dict.fromkeys()). 3 deferred (leg-spread guard like triangulate, baseCcy uncontrolled-select reconcile, future-as_of API clamp). orig: created + dev'd 2026-06-21 (FX cross-rate matrix monitor — Bloomberg-FXC-style square grid of major-currency crosses derived from the USD-base fx_rate star: cell = quote_rate/base_rate via the USD pivot, diagonal 1.0; per-currency as-of staleness (ok/stale/no_data, TWD lags); as-of date picker. New GET /api/sym/fx/matrix (N fx_rate resolutions + grid by division, no N²) + new /monitor/fx page. Functional reproduction only, no Bloomberg IP; derive-don't-store + as-of honesty; reuses WEI/Monitor idioms.)
  wei-backdate-as-of-date: done  # 2026-06-22 code-reviewed (round 3) -> done. Review patch: as-of SQL placeholder %(as_of)s -> canonical %(as_of_date)s (spec names "SQL placeholder"); test string-assertions reconciled. orig: created + dev'd 2026-06-21 -> review (backdate the WEI board to any as_of_date — parameterise index_board() per-index anchor = last session <= as_of + prior; 1D..5Y + 52w re-based by clipping each series to <= as_of and reusing _trailing_returns/_period_return unchanged; optional ?as_of_date= query param; date control on monitor/wei with Latest default + effective-date header. Omitted => unchanged latest board. Read-only, no migration; canonical as_of_date, per-market as-of resolution, EOD honesty.)
  wei-world-equity-indices: done  # 2026-06-22 code-reviewed -> done. Review patch: stale ● tooltip asserted "(market holiday)" as fact (could be a data gap) -> honest reword. 4 deferred (sparse-index 1D gap, pre-history empty-state copy, MSCI single-country region, d5-calendar-days/null-sort cosmetics). orig: created + dev'd 2026-06-21 -> review (World Equity Indices monitor — Bloomberg-WEI-style regional board over index_levels: Americas/EMEA/Asia-Pacific/Global sections, per-index last/net-chg/%chg/YTD + sparkline, up/down colour. New GET /api/sym/indexes/board + region on the benchmark registry + new /sym/wei page. Functional reproduction only, no Bloomberg IP; EOD honesty; reuses the indexes gateway/page conventions.)
  non-brazil-gics-surgical-close: done  # 2026-06-22 (bmad-create-story + dev + verify). The long-standing "non-Brazil GICS gap" (134-row figure was obsolete — sec_sic/yahoo/wikidata closed it to 99.1%). Surgical close of the live residual: FB spurious ProShares-ETF member reversed out of sp500 (reverse_change, 502->501, META kept; sweep clean — WT false positive); new `manual` operator-asserted classification source (rank 1, above automated/below FD primary, always-on, fill-only) closes the 3 FTSE-100 investment trusts (SMT/PCT/ALW) -> Financials (yahoo *.L is 404 in-env). Coverage 99.1->99.3%; ftse100+sp500 universe_member_completeness PASS; live-member unclassified residual = 0. 838 sym tests + 9 new manual tests green; ruff clean.
  wei-add-regional-indices: done  # 2026-06-22 code-reviewed (3 layers) -> done. Auditor all ACs met; 2 low patches (STOXX 600 country Eurozone->Europe; levels.py reflowed ruff-clean — original commit was E501-dirty, Task 5 hadn't run ruff on it). orig dev: 2026-06-22 dev complete -> review. Added Hang Seng (^HSI), CSI 300 (000300.SS), STOXX Europe 600 (^STOXX) to BENCHMARKS + loaded via sym benchmarks -> auto-surface on Indexes + WEI (29 indexes now; CDP-verified both pages, right regions). MSCI EM confirmed already present (no dup); FTSE All-World/Emerging deferred (no free FTSE Russell index source; duplicate MSCI ACWI/EM; no ETF proxy). 841 sym + 15 benchmark tests green. orig: created 2026-06-22 (bmad-create-story, "add new indices to wei page"). Reframe: MSCI Emerging Markets already seeded (MSCI EM Net 891800 — confirm only); FTSE All-World/Emerging deferred (no free FTSE Russell index source; duplicate the present MSCI ACWI/EM; derive-don't-proxy bars an ETF stand-in). New: Hang Seng (^HSI/HKD/Asia-Pacific), CSI 300 (000300.SS?/CNY/Asia-Pacific — probe symbol), STOXX Europe 600 (^STOXX/EUR/EMEA) via Benchmark entries + `sym benchmarks` → auto-surface on Indexes + WEI (data-driven). Probe each yahoo symbol in-env before adding.
  fx-matrix-live: done  # 2026-06-22 code-reviewed (3 adversarial layers) -> done. Auditor: all 8 ACs + 6 conventions PASS, no High/Med. 3 patches: (1) null chg on any cell touching a stale leg (prior=None) — kills misleading 0.00%/half-true move, heat colours only fully-live crosses; (2) exclude USD from priced/total so an all-miss board reads unavailable not delayed-1/16 (matches index_board_live), total==0 guard; (3) all-unavailable + all-priced + JPY-as-base tests. 165 api green; ruff clean; live API now 15/15. Both Blind-Hunter "High"s dismissed (zero-divide unreachable — FX rates strictly positive; spinner/abort matches reviewed WEI sibling). orig 2026-06-22 dev complete -> review. Factored _fx_grid (shared EOD/LIVE cross-derivation, EOD byte-identical) + fx_matrix_live() (USD{ccy}=X live legs as float, USD=1.0, EOD fallback+unavailable, chg=live-cross-vs-latest-EOD-cross, per-currency freshness + rollup) + GET /api/sym/fx/matrix/live (FxMatrixLive/FxCurrencyMetaLive, 503 on dead provider) + LIVE/EOD toggle + live badge + per-currency freshness markers + autoSec auto-refresh on /monitor/fx. 163 api + 135 web green; ruff/tsc/eslint clean; api-types regenerated. Live API 16/16 priced; real-Chrome CDP verified toggle/badge/freshness/grid-rederive/control-swap. PROBE: USD{ccy}=X = currency-per-USD (matches fx_rate). orig 2026-06-22 created  # 2026-06-22 created (bmad-create-story, Andre: "I also want my FX matrix to be live similar to wei page"). Apply the proven wei-live-board LIVE pattern to /monitor/fx: new GET /api/sym/fx/matrix/live (live legs from USD{CCY}=X quotes — PROBED in-env = currency-per-USD, matches the fx_rate convention; USD=1.0; crosses re-derived not scaled; live 1D = live-cross vs latest-EOD-cross; per-currency freshness + matrix rollup; EOD fallback on a missing leg; never persisted) reusing QH.2 quotes.py + the index_board_live/live_heatmap shape; LIVE/EOD toggle + live badge + autoSec auto-refresh on /monitor/fx. The follow-up wei-live-board Open Q#4 explicitly flagged. Factor a shared grid build so EOD stays byte-identical. Design tensions captured: 1D base (live-vs-EOD-cross default), auto-refresh default, leg-vs-cell coverage.
  wei-live-board: done  # 2026-06-22 code-reviewed (3 adversarial layers) -> done. Re-base math confirmed correct; 3 patches (rollup freshness degrades to delayed under partial coverage; ↻ spinner no longer sticks on an aborted refresh; eod_last guard tightened to match its positive-close comment), 2 deferred (as_of-null-when-no-epoch mirrors QH.9; test-strengthening), 5 dismissed. orig dev: 2026-06-22 dev complete -> review. index_board_live() (reuses index_board + endpoint-scaling window re-base + QH.2/QH.9 quote fan-out over the yahoo xref) + GET /api/sym/indexes/board/live + LIVE/EOD toggle + live badge + per-row freshness on /monitor/wei. 160 api + 133 web green; tsc/eslint clean; live API 17/23 priced + real-Chrome CDP toggle verified. orig: created 2026-06-22 (bmad-create-story, "wei page also show Live data"). The LIVE follow-up flagged by wei-world-equity-indices Open Q#3. New GET /api/sym/indexes/board/live (live last + live 1D vs latest EOD close + windows re-based to the live mark via the backdate trick + per-index freshness + board rollup, equity-only, never persisted) reusing QH.2 quotes.py + the QH.9 live_heatmap pattern; LIVE/EOD toggle + live badge on /monitor/wei. Index symbols come from the yahoo xref (^GSPC) directly. Design tensions captured: which windows re-base, 1D base, auto-refresh.
  indexes-add-vix: done  # 2026-06-22 code-reviewed (3 adversarial layers) -> done. No High/Med; 2 low patches (IndexSummary.category default to match its comment; honest-framing note reworded to cover the "Since start" stat + dropped a stray React name-check). 3 dismissed. orig dev: 2026-06-22 dev complete -> review. Benchmark.category + category_for + VIX ^VIX entry; index_board() equity-only filter (VIX off WEI board) while indexes() lists it with category; Indexes page frames VIX as a level (note + plain cumulative cards, no CAGR, relabelled monthly). sym benchmarks loaded VIX (sym_id 2218, 9184 levels). 840 sym + 158 api + 132 web green; tsc/eslint clean; live API + real-Chrome dump-dom verified. orig: created 2026-06-22 (bmad-create-story, "add VIX to the indexes page"). Surface CBOE VIX on the sym Indexes page via a benchmark-registry entry (^VIX, reachable — macro already pulls it as MKT:VIX) + index_levels load through `sym benchmarks`. Adds a `category` field so the WEI EQUITY board excludes VIX (up/down colour semantics invert); label VIX honestly as a level (drop annualised-CAGR investment framing). Page is data-driven so the basic add needs no API/page change. Design tensions captured: return-semantics + board-pollution.
  indexes-msci-eod-pull-and-page: done  # created 2026-06-20; implemented + committed 2026-06-21 (commits 5f00af7 pull+page, ce0be9c trailing returns, 75ccb69 marquee/sort/filter, d8aa146 range selector). sym msci-pull direct from MSCI free endpoint; seeded 7 MSCI variants (World PR/NR/GR + ACWI/EAFE/EM/Europe/USA NR) -> 25 indexes on the page; API /api/sym/indexes[/{id}/levels] with trailing YTD/1Y/3Y/5Y; new Indexes page under sym subnav (chart + stats + 1Y/5Y/Max range + filter). 809 sym + 149 api + web tests green; real-Chrome verified. Committed to main; awaits code-review (recommend a fresh LLM). orig: (pull MSCI index EOD levels DIRECTLY from MSCI's free public getLevelDataForGraph endpoint — verified reachable, variants STRD/NETR/GRTR, history floor 1997 — into index_levels via B4's immutable loader, one instrument per variant; backfill MSCI World NR; read API GET /indexes[/{sym_id}/levels]; new console Indexes page with a level time-series chart. Replaces the rejected chatbot-scraping idea with the authoritative free source.)
  portfolios-nested-grouping: done  # created + implemented + reviewed 2026-06-20 (multi-level/nested grouping — groupBy now ordered string[]; drag a 2nd+ column onto the non-reflowing strip to ADD a nesting level; module-level recursive buildGroups -> GroupNode tree + recursive renderGroupRows with depth indent + path-keyed rows; ["sector"] renders identical to the prior single-level view). Review refinement (AC#10): grouped columns HIDDEN from the grid (visibleOrder) + grouping breadcrumb chips replace the per-header ✕ as the ungroup affordance. Real-Chrome CDP: flat→Sector(3 subtotals,18→17 headers)→Country-nested(6 subtotals/12 rows,→16 headers)→✕Sector(single-level,sector col restored)→✕→flat, header reflow Δ=0px. 14 pivot + 35 portfolio tests green, tsc+eslint clean.
  portfolios-grid-pivot-grouping: done  # created + implemented + reviewed 2026-06-20 (grid FLAT by default; drag a categorical column header onto a drag-only drop strip to pivot; Sector added as a real column; generalised sector grouping via sortValue + the existing pointer-drag; single-level; grouped header carries an ✕ to ungroup). Code-review (3 layers): all 6 ACs + full test matrix confirmed, grand-total refactor proven equivalent; 1 decision + 1 patch + 1 defer + 6 dismissed. Applied: group-by zone → non-reflowing floating overlay (absolute bottom-full, header Δ=0px); stale sector-era prose generalised. Real-Chrome CDP verified no-reflow + flat→drag-Sector→grouped(9)→✕→flat(6). 12 pivot + 33 portfolio tests green, tsc+eslint clean.
  portfolios-column-reorder: done  # created + implemented + reviewed 2026-06-20 (drag header to reorder grid columns; column-descriptor registry drives header + all body rows; colSpan aggregate rows decomposed; Pointer Events drag — switched from native HTML5 DnD after the user found it didn't fire from inside the header button; no new dep; coexists with click-to-sort). Real-Chrome CDP drag verified. Code-review: 8/8 ACs, no correctness bugs; 1 decision (accept insert-before) + 3 patches (listener unmount cleanup+pointercancel, COLUMN_BY_ID guards, dedupe pnlOf). 10 pivot + 24 portfolio tests green, tsc+eslint clean.
