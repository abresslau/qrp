# Decision Log — sym Research Universe brief

- 2026-06-06 — Brief initiated (Create intent). Purpose: define sym's *real* research universe, beyond the ~50-name MVP adversarial seed (`benchmark/seed_universe.toml`). Context carried from the build session: Module 1 (Epics 1–3) complete; identity resolution now supports ISIN-first / ISIN-fallback via OpenFIGI; SM-6 accuracy harness green on the seed.
- 2026-06-06 — Open decisions to resolve in this brief: (1) membership source — index constituents vs vendor ISIN list vs rules-based screen; (2) membership model — point-in-time/SCD (survivorship-safe) vs current snapshot; (3) scale & cadence — hundreds/one-time vs thousands/periodic (gates whether the free yfinance path suffices or a licensed source e.g. EODHD is required).
- 2026-06-06 — RESEARCH FINDING (universe sourcing, 20y point-in-time): **S&P 500** PIT membership is FREE to 2006 (Wikipedia-revision-history repos: fja05680/sp500, riazarbi/sp500-scraper) — membership only, survivor data via yfinance, but delisted *leavers* need a paid source. **Russell 3000** PIT is NOT free and NOT in EODHD (S&P/DJ-only); only individual path is **Norgate Platinum ~$630/yr** (clean PIT to 2000). **MSCI World** PIT is effectively UNAVAILABLE to individuals (MSCI licenses constituents; no retail vendor; iShares URTH proxy is current-only and only since 2012). Survivorship-free 20y union of {S&P 500 ∪ Russell 3000} ≈ **~5,000 distinct US names** (S&P 500 ⊂ Russell 3000); + true MSCI World → ~7–8k but not obtainable. Implication: the three-index ask is not fully achievable at individual budget; the hard/expensive part is always the delisted leavers + non-US names, exactly where free identity/price resolution breaks (cf. the 5 seed delistings — only 2/5 resolved by ISIN).
- 2026-06-06 — DECISION (Andre): adopt **Scope A** as the FIRST universe — free S&P 1500 family (S&P 500/400/600), 20y point-in-time, US-only. MSCI World and Russell 3000 explicitly OUT of v1 (cost/licensing).
- 2026-06-06 — DECISION (Andre): the core deliverable is a **pluggable universe layer** so sources slot in behind one abstraction. v1 must support THREE provider types: (1) **indexes** (free ones only for now), (2) **custom lists**, (3) **criteria-based** screens. Design to mirror sym's existing AR-5 OHLCV source abstraction (Protocol + config-keyed registry).
- 2026-06-06 — RESEARCH FINDING (global index availability, by Andre's request — Ibovespa/IBX/SMLL/DAX/FTSE etc.): free **current** membership is feasible for ~every major market via THREE archetypes — Wikipedia component tables (flagship indexes), exchange-published CSVs (B3 Brazil with weights; **NSE India with ISIN**; JPX/KRX/TWSE/BMV), and ETF-holdings (big/gated: Russell, STOXX 600). Free **point-in-time** is short: turnkey only for S&P 500 (community repos); DIY Wikipedia-diff for ≤50-name flagships (DAX/FTSE100/CAC40/etc.); none for broad/whole-market or most Asian indexes. DESIGN IMPLICATION: index provider should be **archetype-based (Wikipedia / exchange-CSV / ETF-holdings), parameterized by index — not one provider per index** → adding DAX/Ibovespa/Nifty is config, not code. Brazil (B3) & India (NSE) are the cheapest non-US adds. Trademark note: index names are marks; use public/Wikipedia/ETF sources, personal use, don't market as official.
- 2026-06-06 — DECISION (Andre): scope = **US + Europe only**. NO India, NO Brazil/Asia for now (same archetypes enable them later). Start with **S&P 1500**, then European flagships (DAX, FTSE 100/250, CAC 40, EURO STOXX 50, IBEX 35, FTSE MIB, AEX, SMI; STOXX Europe 600 via ETF).
- 2026-06-06 — DECISION (Andre): **daily monitoring of index changes is a core v1 capability** (not define-once). A scheduled daily monitor detects joiners/leavers and records them into the point-in-time membership SCD; sanity-gated (AR-9-style two-stage) so a bad parse can't wipe a universe. This also builds PIT history forward for indexes lacking a free historical source.
- 2026-06-06 — DECISION (Andre): **sequencing = maintenance-first.** Get index membership maintained correctly, THEN drive price ingestion from the maintained set. Proposed epics: U1 foundation, U2 index providers (US+Europe), U3 daily maintenance, U4 universe-driven ingestion, U5 criteria (fast-follow). U1–U3 = index maintenance; U4 = pull prices.
- 2026-06-06 — DECISION: criteria provider is a **fast-follow** (needs market-cap/shares data sym lacks); v1 ships index + custom-list. Daily-monitor guard threshold (auto-apply vs flag) left as a tunable open question.
- 2026-06-06 — SOURCE-STRATEGY decision (advanced-elicitation, method 36 "Challenge from Critical Perspective" + Andre's "leverage Yahoo / open APIs" directive; verified via research). Findings: **Yahoo/yfinance = prices + corroboration only, NOT a membership source** (no reliable constituents). **FMP free tier IS a structured US membership feed** — S&P 500/Nasdaq/Dow current + `historical/sp500_constituent` with dated add/remove events → preferred for US flagships, beats Wikipedia scraping (US-only; free tier may paywall — verify; archetype fallback mitigates). **OpenBB** wraps FMP (US-only, current-focused). **No free structured API for European constituents** — free path is self-archived ETF-holdings CSVs (preferred, less brittle, PIT-forward) + Wikipedia fallback; `yfiua/index-constituents` covers only DAX/FTSE100/FTSE MIB since ~2023; EODHD is the paid European-breadth lever. DECISION: index archetypes reordered to **API-first (US) / ETF-holdings-first (Europe) / Wikipedia-fallback**, structured-API-first to reduce brittleness; Yahoo's role fixed as prices+corroboration.
- 2026-06-06 — FEYNMAN pass (advanced-elicitation, method 45): plain-language explanation held up (and made `pit_valid_from` feel self-evident — "you can't know membership before the diary starts"). One real gap surfaced: **effective-date precision** — API events are exact-dated, snapshot-diff events are only *bounded by the polling gap*; added `effective_date_precision` (exact|poll_bounded) to the event log, with daily polling capping poll_bounded uncertainty at ≤1 day. Also pinned "cleanest plug per index" = **configured per-index source preference + automatic fallback** (FMP→ETF→Wikipedia).
- 2026-06-06 — HINDSIGHT reflection (advanced-elicitation, method 49) surfaced the under-specified *consumption/operation* half (brief was build/maintain-heavy). Added: **as-of membership query API** as the research primitive (`members(universe,date)` ⋈ fact_returns); **multiple concurrent universes + set operations** (membership is many-to-many); **reproducible study snapshots** (`universe @ log-version` → identical reruns after later corrections — sym's determinism ethos extended to the universe); **`universe review` operator digest** (pending changes/stale monitors/aging-unresolved/accuracy alarms). Placed in U1 (API/multi-universe/snapshots) and U3 (ops digest) + 1 new success criterion.
- 2026-06-06 — CONTRACT pass (advanced-elicitation, method 44): right-sized brief.md from ~120 to ~95 lines toward the 1–2 page target — compressed Solution archetype/hardening sub-bullets and Open Questions to summaries + pointers; no content lost (detail lives in addendum §A/§B/§B2/§G/§H + the matrix). Comparative matrix added next to ADR-2 (method 33).
- 2026-06-06 — FMEA pass (advanced-elicitation, method 35) → addendum §H (per-component failure→prevention). New findings beyond the pre-mortem: project membership at **CompositeFIGI level not raw ticker** (a mid-membership ticker rename would otherwise read as leave+rejoin = spurious survivorship gap); ETF diff on **identifier set not weights** + drop non-equity rows + proxy provenance; multi-source **event dedupe + date-precedence**; **joiner triggers historical backfill** (forward-only leaves a hole) — elevated to the brief's ingestion bridge / U4; per-index freshness; aging-unresolved metric; accuracy-gate must use genuinely independent sources. Two findings elevated into brief.md (joiner backfill; FIGI-level projection); rest in addendum §H.
- 2026-06-06 — CRITIQUE-and-refine pass (advanced-elicitation, method 42): fixed consistency defects from the layered edits — "three parts"→"four parts"; merged two contradicting criteria Open-Questions bullets into one resolved (fast-follow) statement; added the FMP/open-API archetype + event-log framing to Scope "In"; neutralized the accuracy-gate source example. No substantive scope change.
- 2026-06-06 — FIRST-PRINCIPLES reframe (advanced-elicitation, method 39). Stripped two assumptions: "membership = SCD" and "monitor = poller". Truths: index membership changes only via discrete dated EVENTS → the source of truth is an **append-only membership-change event log; the `universe_membership` interval (SCD) table is a derived projection**. Polling vs event-driven dissolves (polling = event-discovery for snapshot-only sources; dated APIs emit events directly). A universe is a **predicate over (security, time)**, unifying provider kinds: **event-producing** (index/list) vs **function-evaluating** (criteria = compute `members(date)`, optionally snapshot). Reuses sym's AR-6 (explicit events) + AR-10 (immutable+correction) → reversibility/corroboration fall out for free. Captured as ADR-8 (subsumes ADR-3/ADR-4 framing). Andre confirmed the deeper model over a plain mutable SCD.
- 2026-06-06 — ADRs captured (advanced-elicitation, method 20) into addendum §G: ADR-1 provider abstraction (Protocol+registry, AR-5), ADR-2 archetype priority (API-first US / ETF Europe / Wikipedia fallback), ADR-3 PIT SCD + `pit_valid_from`, ADR-4 daily-poll CDC + corroboration/gate, ADR-5 frozen as-of ISIN-preferred resolution, ADR-6 retain-and-flag, ADR-7 maintenance-first. Provisional; architecture workflow to formalize.
- 2026-06-06 — PRE-MORTEM hardening applied to the brief (advanced-elicitation, method 34). Six failure modes → requirements: (#1) monitor **freshness heartbeat**, empty parse = error not "no change"; (#2 — the big one) **`pit_valid_from` per universe** + guardrail refusing/flagging membership queries before trustworthy history begins (kills silent back-projection of today's members onto the past — the worst survivorship trap, acute for the European build-forward indexes); (#3) resolution **as-of membership date, ISIN-preferred, FIGI frozen at first resolution** (defeats ticker recycling); (#4) **cross-source corroboration + reversible audit trail** for change events; (#5) **ingestion scale (~2k×20y) + coverage visibility** so partial loads can't look complete; (#7) **membership accuracy gate** (Wikipedia-vs-ETF cross-check) → new Epic U3.5. Share-class ambiguity reuses Story 1.6 `share_class_conflict`.
